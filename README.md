# 配方助手

这是一个适合手机使用的离线配方记录页面。

## 使用

直接打开 `index.html`，即可新增、编辑、删除和搜索配方。配方数据仅保存在当前浏览器的本地存储中。

如需将它作为可安装的手机网页应用，请把此文件夹部署到任意 HTTPS 静态网页服务，再使用浏览器的“添加到主屏幕”或“安装应用”。

## 页面行为

- 记录列表按最近编辑时间排列。
- 每条记录的左列为配方名称，右列为具体配方，宽度约为 3:7。
- 搜索同时匹配名称、具体配方和标签，并高亮关键词。
